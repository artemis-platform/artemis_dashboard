<%=
  render_data_table(
    @conn,
    @incidents.entries,
    allowed_columns: data_table_allowed_columns(),
    default_columns: ["source_uid", "triggered_at", "title", "status", "severity", "tags"]
  )
%>
